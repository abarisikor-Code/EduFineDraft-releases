# EduFineDraft 업데이트 안내 파일

EduFineDraft(에듀파인 기안 초안 도우미)가 시작할 때 `latest.json` 만 읽어 새 버전이 있는지 확인합니다.
프로그램 본체·소스는 여기에 없습니다. 내려받기는 `latest.json` 의 `url`(공유 드라이브)에서.

```json
{"version": "0.3.0", "url": "https://...", "notes": "바뀐 점"}
```
